# Deny all direct HTTP access to this folder and its files
# Works on Apache; ignore on other servers
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Deny from all
</IfModule>
